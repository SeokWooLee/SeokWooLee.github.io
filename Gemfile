source "https://rubygems.org"

# GitHub Pages 프로덕션 빌드와 동일한 환경으로 로컬 빌드/미리보기를 하기 위한 구성
# 사용법: bundle install && bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins

gem "webrick" # Ruby 3.0+에서 jekyll serve에 필요
